---
export const prerender = false;

const category = String(Astro.params.category ?? "").trim();
const target = new URL("/mall", Astro.url);
if (category) target.searchParams.set("category", category);
return Astro.redirect(target.toString(), 302);
---
